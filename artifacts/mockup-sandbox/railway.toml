[build]
builder = "DOCKERFILE"
dockerfilePath = "artifacts/mockup-sandbox/Dockerfile"

[deploy]
startCommand = "serve -s ./public -l $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
