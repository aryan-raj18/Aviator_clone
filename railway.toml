[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/aviator-game run build"

[deploy]
startCommand = "pnpm --filter @workspace/aviator-game run serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
